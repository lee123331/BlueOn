/* =========================================================
   🌐 Global Config
   - 전역 공통 설정 파일
   - 모든 JS에서 재선언 없이 안전하게 사용
========================================================= */

// API 서버 주소 (⚠️ 절대 다른 파일에서 const로 다시 선언하지 말 것)
window.API_URL = "https://blueon.up.railway.app";

// 이후 필요하면 여기만 확장
// window.SOCKET_PATH = "/socket.io";
// window.ENV = "production";
